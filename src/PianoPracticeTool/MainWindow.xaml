<Window x:Class="PianoPracticeTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Piano Practice Tool"
        Icon="Assets/AppIcon.ico"
        Height="900"
        Width="1320"
        MinHeight="720"
        MinWidth="1024"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="54" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#10141C"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid Margin="18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="30"
                            Height="30"
                            CornerRadius="7"
                            Background="{DynamicResource AccentBrush}"
                            Margin="0,0,10,0">
                        <TextBlock Text="P"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="17"
                                   FontWeight="Bold" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Piano Practice" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock x:Name="MidiStatusText"
                                   Text="MIDI: 未接続"
                                   FontSize="10"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="MidiInputMonitorText"
                               Text="入力: —"
                               Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               FontSize="10"
                               Foreground="{DynamicResource TextMutedBrush}" />
                    <Button x:Name="ModeSelectionButton"
                            Content="← モード選択"
                            Margin="0,0,8,0"
                            Padding="12,6"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Click="ModeSelectionButton_Click" />
                    <Button x:Name="SettingsButton"
                            Content="⚙ 設定"
                            Padding="12,6"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Click="SettingsButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl x:Name="PageHost" Grid.Row="1" Margin="18,14,18,18" />
    </Grid>
</Window>
